import { navMainConfig, navSecondaryConfig } from './navigation'
import { projectsConfig } from './projects'
import { teamsConfig } from './teams'
import { usersConfig } from './users'

export const sidebarConfig = {
  navMain: navMainConfig,
  navSecondary: navSecondaryConfig,
  projects: projectsConfig,
  teams: teamsConfig,
  users: usersConfig,
}
